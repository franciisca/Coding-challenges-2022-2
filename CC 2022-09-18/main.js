//Function 2 - squaring an argument
//https://www.codewars.com/kata/523b623152af8a30c6000027/train/javascript

//Now you have to write a function that takes an argument and returns the square of it.

//P. function takes an argument (integers only?)
//R. returns the square of that argument.
//E. 2 => returns 4
//pseudo. function takes an argument. returns it squared. Use Math.pow(number,2)

function square(number){
    return Math.pow(number,2)
}